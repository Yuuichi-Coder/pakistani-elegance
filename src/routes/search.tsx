import { createFileRoute } from "@tanstack/react-router";import { useState } from "react";import { Search } from "lucide-react";import { products } from "@/lib/catalog";import { ProductGrid } from "@/components/sections";
export const Route=createFileRoute("/search")({validateSearch:(s:Record<string,unknown>)=>({q:typeof s["q"]==="string"?s["q"]:""}),head:()=>({meta:[{title:"Search — Maryam Fashions"},{name:"description",content:"Search Pakistani lawn, luxury pret and festive suits."},{property:"og:title",content:"Search — Maryam Fashions"},{property:"og:description",content:"Find your next Maryam Fashions look."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:SearchPage});function SearchPage(){const initial=Route.useSearch().q;const [q,setQ]=useState(initial);const results=products.filter(p=>(p.name+p.fabric+p.color).toLowerCase().includes(q.toLowerCase()));return <div className="mx-auto max-w-[1440px] px-4 py-12 lg:px-8"><h1 className="font-display text-4xl">Search</h1><div className="mt-6 flex max-w-xl items-center gap-3 border-b border-foreground"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search the collection" className="w-full py-3 outline-none"/></div><p className="my-8 text-sm text-muted-foreground">{results.length} results {q&&`for “${q}”`}</p><ProductGrid items={results}/></div>}
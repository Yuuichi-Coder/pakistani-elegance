import { createFileRoute } from "@tanstack/react-router";import { useState } from "react";import { supabase } from "@/integrations/supabase/client";import { lovable } from "@/integrations/lovable";import { Button } from "@/components/ui/button";
export const Route=createFileRoute("/account")({head:()=>({meta:[{title:"Account — Maryam Fashions"},{name:"description",content:"Sign in or create your Maryam Fashions account."},{property:"og:title",content:"Account — Maryam Fashions"},{property:"og:description",content:"Access your Maryam Fashions account."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Account});function Account(){const [mode,setMode]=useState<"login"|"signup"|"forgot">("login");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [msg,setMsg]=useState("");const submit=async(e:React.FormEvent)=>{e.preventDefault();setMsg("");if(mode==="forgot"){const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/reset-password`});setMsg(error?.message??"Check your email for a secure reset link.");return}if(mode==="signup"){const{data,error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:location.origin}});setMsg(error?.message??(data.session?"Account created.":"Check your email to confirm your account."));return}const{error}=await supabase.auth.signInWithPassword({email,password});setMsg(error?.message??"Welcome back.")};return <div className="mx-auto max-w-md px-4 py-20"><p className="text-center text-xs uppercase tracking-[.25em] text-gold">Maryam Fashions</p><h1 className="mt-2 text-center font-display text-4xl">{mode==="login"?"Welcome back":mode==="signup"?"Create account":"Reset password"}</h1><form onSubmit={submit} className="mt-8 space-y-4"><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="w-full border border-input p-3"/>{mode!=="forgot"&&<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full border border-input p-3"/>}<Button type="submit" className="w-full">{mode==="login"?"Sign in":mode==="signup"?"Create account":"Send reset link"}</Button></form>{mode!=="forgot"&&<><div className="my-5 flex items-center gap-3 text-xs text-muted-foreground"><span className="h-px flex-1 bg-border"/>or<span className="h-px flex-1 bg-border"/></div><Button onClick={()=>lovable.auth.signInWithOAuth("google",{redirect_uri:location.origin})} className="w-full bg-background text-foreground ring-1 ring-border">Continue with Google</Button></>}<div className="mt-6 flex justify-center gap-4 text-xs">{mode!=="login"&&<button onClick={()=>setMode("login")}>Sign in</button>}{mode!=="signup"&&<button onClick={()=>setMode("signup")}>Create account</button>}{mode!=="forgot"&&<button onClick={()=>setMode("forgot")}>Forgot password?</button>}</div>{msg&&<p role="status" className="mt-5 text-center text-sm text-primary">{msg}</p>}</div>}